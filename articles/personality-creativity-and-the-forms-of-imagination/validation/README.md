# Validation

Run from the article directory:

```bash
bash validation/run_validation.sh
```

The validation script checks required files, expected dataset structure, and minimum synthetic row count.
