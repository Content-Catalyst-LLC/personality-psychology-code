# Python Workflow

Run from the article directory:

```bash
python3 python/analyze_personality_creativity.py
```

Recommended dependencies:

```bash
pip install pandas numpy statsmodels matplotlib
```

The script writes descriptive statistics, correlation matrices, regression coefficients, and figures to `outputs/`.
