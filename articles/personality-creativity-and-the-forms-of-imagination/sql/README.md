# SQL Workflow

Run from the article directory:

```bash
sqlite3 outputs/tables/personality_creativity.sqlite < sql/schema_and_queries.sql
```

The SQL example creates a table, imports the synthetic CSV, and runs domain-level summaries.
